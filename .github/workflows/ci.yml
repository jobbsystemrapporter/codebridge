name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm ci && cd ui && npm ci
      - name: Build native helper (arm64)
        run: |
          cd native-helper
          swift build -c release --arch arm64 --scratch-path .build/arm64
          mkdir -p .build/release
          cp .build/arm64/release/codebridge-helper .build/release/codebridge-helper
      - name: Build renderer
        run: cd ui && npm run build
      - name: Test suite
        env:
          CODEBRIDGE_HELPER: "${{ github.workspace }}/native-helper/.build/release/codebridge-helper"
        run: |
          node --check src/server.mjs
          npm test
          node test/first-run-test.mjs
          node test/access-mode-test.mjs
          node test/security-regression-test.mjs
          node test/launchagent-test.mjs
          node test/transport-test.mjs
          node test/parity-test.mjs
          node test/compat-test.mjs
          node test/repo-check.mjs
          node test/privacy-check.mjs

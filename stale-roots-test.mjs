import assert from'node:assert/strict';import fs from'node:fs/promises';import os from'node:os';import path from'node:path';const home=await fs.mkdtemp(path.join(os.tmpdir(),'cb-state-')),real=await fs.mkdtemp(path.join(os.tmpdir(),'real-project-')),stale=path.join(os.tmpdir(),'codebridge-e2e-dead123');process.env.CODEBRIDGE_HOME=home;await fs.mkdir(home,{recursive:true});await fs.writeFile(path.join(home,'config.json'),JSON.stringify({version:1,allowedRoots:[real,stale,path.join(os.tmpdir(),'cbx-oldtest')],setupComplete:false}));const{getConfig}=await import('./core.mjs');const c=await getConfig();assert.deepEqual(c.allowedRoots,[real]);const persisted=JSON.parse(await fs.readFile(path.join(home,'config.json'),'utf8'));assert.deepEqual(persisted.allowedRoots,[real]);console.log('Stale/test project cleanup passed.');await fs.rm(home,{recursive:true,force:true});await fs.rm(real,{recursive:true,force:true});
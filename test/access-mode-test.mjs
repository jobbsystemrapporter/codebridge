import assert from'node:assert/strict';import fs from'node:fs/promises';import os from'node:os';import path from'node:path';process.env.CODEBRIDGE_HOME=await fs.mkdtemp(path.join(os.tmpdir(),'codebridge-access-'));const{tools,callTool}=await import('../src/mcp.mjs');const{setAccessMode,getAccessMode,toolAllowed}=await import('../src/capabilities.mjs');assert.equal(await getAccessMode(),'read-only');assert.equal(toolAllowed('read_file','read-only'),true);assert.equal(toolAllowed('write_file','read-only'),false);await assert.rejects(()=>callTool('write_file',{path:'/tmp/no',content:'x'}),/read-only mode/);await setAccessMode('full-coding');assert.equal(await getAccessMode(),'full-coding');assert.ok(tools.some(t=>t.name==='write_file'));console.log('Access mode: safe default + write gating passed.');
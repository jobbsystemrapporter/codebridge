import fsp from 'node:fs/promises';import os from'node:os';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.dirname(fileURLToPath(import.meta.url)),src=await fsp.readFile(path.join(root,'launchd','com.codebridge.app.plist'),'utf8'),dst=path.join(os.homedir(),'Library','LaunchAgents','com.codebridge.app.plist');await fsp.mkdir(path.dirname(dst),{recursive:true});await fsp.writeFile(dst,src.replaceAll('__CODEBRIDGE_ROOT__',root));console.log(`Installed launch agent: ${dst}`);console.log('Load it with: launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.codebridge.app.plist');

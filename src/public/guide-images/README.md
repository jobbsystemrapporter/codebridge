# ChatGPT guide screenshots

Drop current real ChatGPT screenshots here so the onboarding guide shows them
automatically. The app uses the first file that exists for each step:

- `ngrok-authtoken.png` – ngrok dashboard with the authtoken to copy
- `chatgpt-editor.png` – GPT editor (Name/Description/Instructions area)
- `chatgpt-features.png` – Features with Code Interpreter & Data Analysis
- `chatgpt-create-action.png` – Actions > Create new action
- `chatgpt-schema.png` – Action Schema field
- `chatgpt-auth.png` – Authentication: API Key + Bearer

Take the screenshots from the current ChatGPT layout, crop them, and make the
click target obvious. They are optional: until a file exists, the guide shows
a clearly-marked illustration instead.

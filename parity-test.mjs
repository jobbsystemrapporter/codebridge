import assert from'node:assert/strict';import{tools}from'./mcp.mjs';import{requiredParity}from'./devspace-parity.mjs';const names=new Set(tools.map(x=>x.name));for(const n of['open_workspace','list_files','read_file','write_file','run_command'])assert.ok(names.has(n),`missing ${n}`);const p=requiredParity();assert.ok(p.some(x=>x.id==='remote'));assert.ok(p.some(x=>x.id==='autostart'));console.log(`DevSpace replacement contract: ${p.length} required capabilities tracked; core tool parity passed.`);